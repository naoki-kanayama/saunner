<%= render "shared/header" %>
  <div>
    <h1><%= @post.prefecture.name %>の投稿一覧</h1>
  </div>
  <% @post.images.each do |image| %>
    <%= image_tag image.variant(resize: '500x500') %>
  <% end %>
<%= render "shared/footer" %>